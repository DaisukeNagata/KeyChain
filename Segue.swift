//
//  Segue.swift
//  KeyChain
//
//  Created by 永田大祐 on 2017/05/08.
//  Copyright © 2017年 永田大祐. All rights reserved.
//

import Foundation
import UIKit
/*

func memberLogin() {
    let check =  DefaultsManager.defaultsManager.defalut.object(forKey: "bool") as? Bool
    if check == false {
        let storyBord = UIStoryboard(name:"TateruWeb",bundle: Bundle.main)
        let segueSet = storyBord.instantiateViewController(withIdentifier: "TateruWebView") as! TateruWebViewController
        segueSet.transitioningDelegate = self
        self.present(segueSet, animated: false, completion:nil)
    }
}

func tateruLogin(){
    let check =  DefaultsManager.defaultsManager.defalut.object(forKey: "bool") as? Bool
    if check == false {
        let storyBord = UIStoryboard(name:"TateruLogin",bundle: Bundle.main)
        let segueSet = storyBord.instantiateViewController(withIdentifier: "TateruLogin") as! TateruLoginViewController
        segueSet.transitioningDelegate = self
        self.present(segueSet, animated: false, completion:nil)
    }
}
*/
